@{
    ViewData["Title"] = "Home Page";
}

<div id="home-app"
     class="flex flex-col md:h-full"
     data-books-url="@Url.Action("Index", "Books")"
     data-authors-url="@Url.Action("Index", "Authors")"></div>
